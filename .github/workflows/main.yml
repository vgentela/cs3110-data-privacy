
name : Git Pull
on :
  schedule :
    - cron : '0 0 * * 0,1'
jobs:
  git-pull :
    runs-on : ubuntu-latest
    steps:
    - name : checkout code
      uses : actions/checkout@v2
    - name : Git Pull
      run : git pull
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN}}
